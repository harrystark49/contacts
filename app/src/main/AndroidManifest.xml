<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.contacts">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Contacts">
        <activity android:name=".MainActivity"
            android:theme="@style/main">

        </activity>
        <activity android:name=".splash"
            android:theme="@style/spalsh">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".Register"></activity>
        <activity android:name=".login"></activity>
        <activity android:name=".Add_Contact"></activity>
        <activity android:name=".display_numbers"></activity>
        <activity android:name=".delete_contact"
            android:theme="@style/spalsh"></activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>